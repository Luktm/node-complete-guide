<%-include('../includes/head')%>

<body>

<%-include('../includes/navigation')%>

<main class="centered">
    <h1><%= product.title %></h1>
    <hr>
    <div>
        <img src="<%= product.imageUrl %>" alt="<%= product.title %>">
    </div>
    <h2><%= product.price %></h2>
    <h2><%= product.description %></h2>
    <!-- this no need for second argument to pass props, bcuz product here is globally -->
    <!-- but i added it just in case i forgot -->
    <%- include('../includes/add-to-cart.ejs', {product: product}) %>
</main>

<%-include('../includes/end')%>
